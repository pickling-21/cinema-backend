# Changelog

- Auth API: signup, login, refresh, logout (JWT access + refresh токены)
- Ролевая модель: grant, revoke, check (admin, subscriber, member)
- CLI-команда `createsuperuser` для создания суперпользователя
- Тесты всех эндпоинтов (pytest + httpx)
- Docker Compose + docker-compose.tests.yaml